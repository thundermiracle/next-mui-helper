import { deepOrange, teal } from '@material-ui/core/colors';

const theme = {
  palette: {
    primary: deepOrange,
    secondary: teal,
  },
  typography: {
    useNextVariants: true,
  },
};

export default theme;
